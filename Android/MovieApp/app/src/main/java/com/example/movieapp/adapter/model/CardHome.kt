package com.example.movieapp.adapter.model

import android.widget.Button
import android.widget.ProgressBar
import android.widget.TextView

data class CardHome(val id: Int, val title: String, val view: Int, val widthCard: Int, val heightCard: Int, val isBorderImage: Boolean, val progressbarId: Int, val viewAllId: Int, val titleGenreId: Int)